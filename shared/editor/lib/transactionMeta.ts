/**
 * Meta key for editor transactions that update ProseMirror internal state but
 * should not force the outer React editor tree to render.
 */
export const skipReactUpdateMeta = "skipReactUpdate";
